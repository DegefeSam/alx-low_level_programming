#include "main.h"

/**
 * mul - entry point of the function to multiply two numbers
 * Return: always 0 or multiple
 *@x: first number
 *@y: second number
 */
int mul(int x, int y)
{
	int pro;

	pro = x * y;
	return (pro);
}
